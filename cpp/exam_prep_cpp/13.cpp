#include<bits/stdc++.h>
using namespace std;
int main()
{
    string s;
    cin.clear();
    getline(cin,s);
    cin.ignore();

    sort(s.begin(),s.end());
    reverse(s.begin(),s.end());
    vector<char> v;
    s.at(0);

 
    return 0;
}